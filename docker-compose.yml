services:
  battleship_server:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    ports:
      - "4242:4242"
    volumes:
      - battleship_data:/data
    command:
      - "--address"
      - "0.0.0.0"
      - "--port"
      - "4242"
      - "--database"
      - "/data/battleship.db"
    restart: unless-stopped

volumes:
  battleship_data:
